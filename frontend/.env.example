# AnimeTracker 環境變數設置

## 如何設置 .env 文件

1. 複製此文件為 `.env.local`
2. 填入你的 API 密鑰和 Google Apps Script URL

```bash
cp .env.example .env.local
```

## 環境變數說明

### VITE_TMDB_API_KEY
- **說明**：TMDB API 密鑰，用於獲取動漫和電視劇資料
- **取得方式**：
  1. 訪問 https://www.themoviedb.org/
  2. 註冊帳號（如果還沒有）
  3. 進入 Settings > API
  4. 複製 API Key (v3 auth)

### VITE_GAS_DEPLOYMENT_URL
- **說明**：Google Apps Script 部署的網址，用於將進度同步到 Google Sheets
- **取得方式**：
  1. 部署 GAS 腳本（見 `/gas/README.md`）
  2. 複製部署後的 Web App URL
  3. 例如：`https://script.google.com/macros/d/{DEPLOYMENT_ID}/usercallable`

### VITE_SPREADSHEET_ID
- **說明**：Google Sheets 試算表 ID
- **取得方式**：
  1. 在 Google Sheets 中創建新試算表
  2. URL 中的 `https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit`
  3. 複製 `{SPREADSHEET_ID}` 部分

### VITE_APP_NAME
- **說明**：應用名稱（可選）
- **預設值**：AnimeTracker

### VITE_API_BASE_URL
- **說明**：API 基礎 URL（可選）
- **預設值**：https://api.themoviedb.org/3

## 安全提示

⚠️ **重要**：
- **不要**將包含真實密鑰的 `.env.local` 提交到 Git
- `.env.local` 已在 `.gitignore` 中排除
- 在生產環境中，使用環境變數或密鑰管理服務
